// ボタン/リンク共通の pill スタイル。
// 呼び出し側では幅・余白・文字サイズ（w-full, px-*, text-* 等）だけを足す。
export const pillPrimary =
  "rounded-full bg-ink text-center font-bold text-paper shadow-lg transition-transform hover:scale-[1.02] active:scale-95";

export const pillSecondary =
  "rounded-full border-2 border-ink/15 py-3 text-center text-sm font-bold text-ink/70 transition-colors hover:border-ink/30";
